# LEARNINGS.md — index

GENERATED by `tools/gen-learnings-index.py`. Do not hand-edit; re-run it.

`LEARNINGS.md` is ~800 KB and nobody reads it whole. Scan this index, then jump:

```bash
grep -n "<heading text>" LEARNINGS.md   # then Read with that offset
```

Every heading states its lesson in full, so **the index is the summary** — if a
line does not sound relevant, neither is the section under it. `L<n>` is the line
in LEARNINGS.md at generation time; re-run the tool if the numbers look off.

Roughly half the file is transferable method (the topic groups) and half is
per-backend porting detail (Per-model port notes + the family cross-reference).
Reach for the topic groups when you are stuck on a *technique*, and the
cross-reference when you already know which model you are touching.

## Index by topic (284 lessons)

**Security & untrusted input** (2)

- L1699 — Two more untrusted-input lessons: an HTTP server reads the body before your auth check, and a parser that abort()s on malformed input is a DoS a fuzzer finds in seconds
- L1726 — Every hand-rolled file parser sizes a buffer from an untrusted length field — that is the #1 memory-safety bug class, and a multi-agent audit finds them fast

**Compliance, provenance & marking (EU AI Act)** (7)

- L442 — A watermarked TTS output cannot be A/B'd with `cmp` — and whisper LID is not an accent metric
- L1002 — Moving a release tag is CLEAN when the fix touches only release TOOLING — the test is provenance, not "never move a tag"
- L11074 — Always rebuild the box-under-test before benchmarking
- L16163 — EU AI Act audit — provenance not suffix
- L16221 — EU AI Act — a PRESET voice can be a real person
- L16671 — consent record — BIND to the audio, don't chain the log
- L17966 — watermark detector — swap the STATISTIC, not the threshold

**Build, CI, release & packaging** (21)

- L346 — A packaging step that destroys the state a later step reads — and scripts that only run in a release job
- L650 — A guard job that runs ONE compiler family guards one compiler family
- L938 — A green release job is not a shipped artifact — verify what you DELIVER, not what the workflow says
- L1067 — "Linked in CMake" is NOT evidence the code SHIPS — the linker drops an object nothing references
- L1287 — Path-filtered CI workflows ROT — the first re-triggering change inherits ALL the debt accumulated since the last run
- L1307 — Scoped `ctest -R` on a PARTIALLY BUILT tree has two name-collision traps: configure-time add_test and catch_discover NOT_BUILT placeholders
- L1592 — Porting a libllama-hosted reference onto our in-house Qwen3 was clean on first contact; and Kaggle `kernels_logs()` is the escape hatch when the output is page-capped
- L1650 — ggml disables CUDA graphs below Ampere (cc < 800) — an Ampere-only re-warm bug can't be reproduced on Kaggle's P100/T4
- L1949 — Two concurrent HuggingFace uploads from one machine cause spurious mid-batch failures — serialize them (#192 aligner re-ship)
- L2418 — Never benchmark on a near-full disk — SIGBUS + nondeterminism masquerade as a code signal (§192)
- L2494 — Kaggle: datasets are per-account, and the ccache dataset has a required shape (§213)
- L2916 — Kaggle regression kernels: write output incrementally, crash-guard everything
- L3043 — Regenerate Go cgo LDFLAGS from a *linux-equivalent* config, never macOS
- L3058 — cmake-js defaults its build dir to `build/` — point it elsewhere (§166)
- L3212 — HF Space is a separate, FLAT repo that silently drifts from `hf-space/`
- L4515 — Windows / MSVC portability
- L5753 — GitHub Actions workflow triggers — `master` → `main` rename gotcha
- L5899 — libstelnettts.a + libcommon.a both define stb_vorbis / miniaudio impl (Linux ld dies)
- L11138 — Kaggle as a batch-rebake target: seven fragilities the script has to work around
- L14582 — CI has no NVIDIA GPU — a CUDA-only graph path (mimo RVQ) is guarded by an on-Kaggle exact-parity smoke, not CI (#309)
- L15409 — #313 Rust crates on crates.io via git dep

**Multi-surface wiring — CLI / C ABI / bindings / server** (27)

- L113 — Four ways a measurement lied in one session — and the experiment to run when you lack the reporter's hardware
- L164 — A guard that no toolchain runs, and a test literal that pins the MODEL instead of the feature
- L213 — Splitting a path in half drops its prologue and its epilogue, and "disjoint state" has to include what the CALLER does between the halves
- L571 — A gate that ANDs "both sides known" is only as alive as its weakest detector
- L810 — A new REQUIRED request field is a breaking API change on a schedule you don't control — deny the sub-feature, don't refuse the request
- L898 — A hardcoded decode cap silently ignores --max-new-tokens — and forwarding it naively SHRINKS a backend
- L975 — A capability flag is a PROMISE the backend must keep — an unimplemented cap disables the safety nets keyed off it
- L1269 — Two writers on one output field at different pipeline stages = an ordering bug — fix the ORDER, don't bolt on provenance
- L1328 — A binding wrapper that has never been EXECUTED is untested code — and parallel agents produce cross-cutting conflicts only the verifier can see
- L1918 — A persistent KV cache reused from a larger allocation makes local `max_ctx` the wrong stride (#171 VibeVoice server leak)
- L2825 — Backend-name guards must match the *registered* name, by prefix when aliased (#171, #174)
- L2959 — A feature has ~8 front-ends — wiring it into one isn't "done" (§166)
- L2985 — A dry-run "preview" must mirror the real resolver, or it lies (§166)
- L3070 — A WebSocket server that "works in the browser" can still be RFC-broken (§166)
- L3084 — Server warmup is the launch-time differentiator vs the CLI (#165)
- L3097 — Don't throw on the per-request server path; surface the real error (#165)
- L6424 — Chatterbox CAMPPlus phase 1 — Kaldi fbank front-end
- L8966 — voxcpm2 VAE — Python wrapper that captures kwargs without forwarding
- L13502 — 2026-07-02 — `--gpu-backend` was silently ignored (#214)
- L14190 — Before starting a "top-priority, profiled" perf target: check whether it's already been tried-and-rejected, and whether a parallel session owns it
- L14671 — A public API with no caller is a harness-blind zone announcing itself — and an ONNX export may FOLD a BatchNorm your C++ still expects as tensors
- L15343 — #311 --strict-pipeline SHIPPED
- L15486 — #315 vibevoice long-form ASR budget: PR fixed only session; CLI+server needed the adapter…
- L15522 — #324 server diarize DROPPED 59% of the transcript
- L17672 — Parakeet #208 long-audio + enc-cache collapse
- L17779 — session-state fixes + moonshine CPU-only SOLVED
- L18003 — Wyoming TTS marked nothing — FIXED 8f4197ec

**Parity, the diff harness & ground truth** (39)

- L61 — A reference that shares the runtime's assumption cannot falsify it — and a green diff is not evidence
- L275 — A guard has to assert the property the CONSUMER reads — segment spans are not cue times
- L615 — The diff harness starts where its INPUT starts — check what it is fed before trusting parity
- L842 — When a clone/zero-shot path misbehaves but the BAKED/preset path is clean, diff the two paths' DATA byte-for-byte — the difference IS the bug
- L1105 — Cosine, correlation and peak-match are ALL scale-invariant — a uniform gain error passes every one of them
- L1173 — A logits-level diff harness at cos 1.000000 says nothing about the TABLE that turns logits into labels
- L1248 — When you can't run the acceptance roundtrip, ship the path gated default-OFF + a provable-equivalence argument — don't block on the model
- L1345 — Deterministic DROPOUT of whole words/tokens (not garble) = a zeroed weight BLOCK in the shipped GGUF — scan converted tensors for zero-norm rows BEFORE debugging the port
- L1390 — A TTS AR model that "never stops" is often a WRONG-SAMPLING-PARAMS bug, not a stop-head/port bug — read the model card's generation params
- L1685 — Validating a TTS perf change by WAV correlation is invalid — melotts `--seed` isn't deterministic; use ASR round-trip or a deterministic-ASR bit-check
- L1767 — MelsTime mel layout IS ggml Conv1d input layout — transposing corrupts with cos ~0.3, not a gradual drift
- L1812 — Baked mel filterbank layout MUST match core_mel's `fb_layout` — a transpose produces cos_min ≈ −0.15, not a gradual drift
- L2015 — A production default that diverges from the upstream reference is a whole class of bug the stage-cosine diff can't see (#192 TADA)
- L2055 — A forced aligner is not an ASR model — its free CTC argmax is all-blank (#192)
- L2064 — When the engine and the PyTorch reference produce the SAME wrong output, the engine is right — the harness input (usually the prompt) is the bug (moss-transcribe)
- L2089 — An input feature the checkpoint wasn't trained with is worse than nothing — the moss-transcribe time-marker experiment (#218)
- L2114 — Stage the PyTorch reference load — encoder and LM as separate standalone modules, never the monolithic `*ForCausalLM` (moss-transcribe)
- L2134 — Parakeet single-pass DROPS whole sections of long audio — chunk-and-merge, and pick the reference clip carefully (#208b)
- L2430 — Always A/B against the *upstream reference*, not just your own option matrix (§216)
- L2481 — TTS WAV md5 is only a valid parity gate with a pinned seed
- L2902 — Regression transcripts need WER tolerance, not byte-exact match (#92)
- L3912 — Methodical debugging of ported models against ground truth
- L6019 — Chatterbox base T3 sampler parity — Gumbel-max → torch.multinomial
- L6058 — Chatterbox HiFT vocoder parity nits
- L9089 — Diff-harness "drift" is mostly the GGUF quant, not a code bug
- L12364 — Pocket TTS — 12 bugs from stub to cos=0.999997
- L13779 — ASR roundtrip does not validate audio ONSETS; reproduce with the original pipeline before debugging your runtime
- L13823 — Porting the missing sub-model is necessary but not sufficient — hunt the hardcoded "unconditional" defaults that ignore it
- L13896 — Python reference dumpers must apply identical audio conditioning as the C++ runtime
- L13913 — A `frames/token` (or chars/sec) length heuristic silently truncates diffusion TTS — run the duration predictor the model already ships
- L14018 — A faithful port can still loop: get the bf16 blueprint's behaviour on the SAME audio before hunting a runtime bug
- L14078 — Generation-config defaults are part of the blueprint contract — a masked-iterative TTS with the wrong sampler knobs degenerates to SILENCE, not to bad audio
- L14222 — CosyVoice3 CFM Euler steps: 6 is the sweet spot, and validate it with log-mel corr vs full-steps — NOT ASR roundtrip
- L14414 — Exact greedy code-parity is unachievable for a QUANTIZED AR audio LM — confirm the port with a step-0 logit-rank probe, not byte-exact codes
- L14464 — A GPU-vs-reference RTF gap that survives kernel parity is per-step HOST detours — fuse them into the step graph; unified-memory Metal hides exactly this class of cost
- L14557 — A normalization stage amplifies upstream float error — input-align it before trusting its cos (§248 MBR)
- L15695 — #333 madlad400 quants + T5 parity
- L16567 — chatterbox hift_pcm(ref_mel) cos≈0.879 was a diff-harness layout bug (source_stft fed transposed), not a…
- L18197 — Chatterbox Multilingual V3: checkpoint names, quant hashes, and a non-silent clone are not parity

**ggml graphs, allocation & caching** (43)

- L881 — Reusing a cached scheduler graph across `sched_reset`/`alloc` cycles is CPU-safe but SIGSEGVs on GPU — the reused input tensor is bound to the prior cycle's freed buffer
- L1451 — Metal-validated GPU TTS graph paths DID transfer cleanly to CUDA — the stricter-CUDA risk is real but not universal
- L1487 — BatchNorm-fold must respect the conv tensor's dtype — and a per-backend fix usually has siblings
- L1521 — "Migrate host KV to device-resident" is a ~1% win on a compute-bound transformer decoder — measure the fraction before the rewrite
- L1821 — Never cache a ggml compute graph across calls that share a scheduler with a larger graph — the allocator regrows and frees the cached graph's GPU buffers
- L1832 — Flow-matching DiT with JointAttention REQUIRES attention masking even for unconditional generation — skipping masked KV tokens silently corrupts output (2026-07)
- L2167 — A cached cgraph is NOT re-entrant with ggml_backend_sched — the 2nd reuse silently corrupts (#208)
- L2209 — On Metal a cached decode graph is dispatch-bound, not alloc-bound — measure the step's parts, don't trust wall time (§210, PR #207)
- L2366 — Precomputed attention masks in a GGUF are dead weight if the runtime rebuilds them (§192)
- L2534 — Cross-attention KV is a free F16 win — encoder-decoder backends (§176i)
- L2562 — Localizing a GPU miscompute with CPU-vs-Metal diffing — the ggml_backend_sched weight-less-first-op trap (§206)
- L2716 — A byte-identical standalone reproducer is the only way to (dis)prove a "ggml bug" (§203)
- L2746 — In-place device-KV decode graphs on Metal: three hazards (§176b+c, §203)
- L2854 — Never pass `ggml_graph_get_tensor` directly to `ggml_backend_tensor_set` (#164)
- L2874 — `rope_theta=0` means "no positional encoding" — skip RoPE entirely (#164)
- L2883 — `ggml_siglu` ≠ PyTorch `glu` — the gate/value halves are SWAPPED (#81)
- L2952 — Asymmetric rel-pos shift uses the same formula as symmetric (#81)
- L2997 — Graph input tensors that nothing consumes are invisible to `ggml_graph_get_tensor` (#164)
- L3030 — `ggml_backend_cpu_set_n_threads` asserts CPU — guard it after `init_best()`
- L3143 — Decompose ConvTranspose1d → mul_mat + col2im_1d; port col2im per-backend (#155)
- L3183 — Beam-search KV snapshots must stay on-device (#161)
- L3466 — ggml / inference engine
- L4038 — ggml graph allocation: gallocr vs compute_with_ctx
- L5386 — 2026-04-22 - No-gpu mode must gate `ggml_backend_load_all()`
- L7491 — 2026-05-05 — ggml fork patches we carry (must re-apply on every ggml bump)
- L8762 — voxcpm2 perf — per-step ggml graphs, Metal, SIMD layouts
- L9046 — ggml broadcast hides size-mismatch bugs
- L11266 — ggml scheduler tightened cross-backend tensor resolution between §56 and 2026-05-26 (PLAN #115)
- L12520 — Pocket TTS — manual CPU → ggml compute graph rewrite
- L12759 — AudioSeal ggml port — ggml_pad_ext convention trap
- L12836 — GPU weight mirrors for mixed legacy / graph codebases
- L12960 — ggml_graph_get_tensor hash invalidated by ggml_gallocr
- L13153 — read_tensor_f32 weight pre-cache for VITS-family TTS
- L13166 — Single-token embed graph elimination for LLM-ASR
- L13680 — A once-allocated-then-reused bucket step graph is a CUDA-graph-capture hazard: reset+alloc the sched EVERY step
- L13739 — A cached cgraph is invalidated by ANY other graph on its scheduler — reset+alloc per step is NOT enough
- L13839 — TTS code predictor: the dispatch overhead is real, but "fuse into one graph" and "skip the reset" are BOTH wrong — the fix is a sched-free persistent graph
- L13908 — Any T-sized `ggml_view` into a fixed-length weight table must be bounded by the tensor's real length — long inputs overflow it and abort
- L14050 — gallocr does NOT protect input-flagged tensors across computes — re-set EVERY input before EVERY compute of a persistent graph
- L14111 — Vocoder/codec conv graphs hide three graph-construction wastes that dwarf the actual conv FLOPs — profile per-node before assuming it's inherent
- L14158 — Before optimizing a graph's *dispatch*, split host-encode vs GPU-execute — and never trust a GPU benchmark taken under load
- L14253 — A "GPU" model can be silently running on CPU — audit for hot graphs pinned to backend_cpu and CLI adapters that never forward use_gpu
- L17741 — PR #244 dequant-cache fix MERGED

**GPU portability — Metal / CUDA / Vulkan** (24)

- L1426 — When the full system needs an unavailable resource (model / GPU), factor the risky logic into a pure helper and prove IT on synthetic data
- L1541 — Verify a roadmap "broken/OPEN" claim empirically before implementing it — the codebase may have outgrown the note
- L1621 — A Metal masked-attention-padding penalty does NOT transfer to CUDA, and a bucket-width change is byte-identical on Metal/CPU but not on CUDA
- L1668 — ggml-metal im2col starves batch-1 convs — 3–11 threads/threadgroup at N=1, ~40× below bandwidth
- L2330 — A "garbled GPU output" bug can live entirely downstream — A/B the GPUs before localizing (§192)
- L2384 — MoltenVK `mul_mm`/`mul_mat_vec` downconvert src0 to f16 regardless of stored dtype (§192)
- L2395 — Vulkan has no `REPEAT f16→f16` — cast K/V to F32 *before* the GQA repeat (§192)
- L2408 — On macOS, GGML_METAL is default-ON and silently wins over Vulkan (§192)
- L2458 — §214 Metal batched (B=2) quantized mat-vec ≠ the single-token PREC_F32 path
- L2669 — Metal's q8_0 mat-vec kernel requantizes activations to q8 and ignores the F32 prec hint (§205)
- L3167 — Measuring GPU perf on a thermally-throttling laptop GPU
- L9412 — WDDM idle-clock-state hysteresis on consumer/laptop NVIDIA SKUs
- L9549 — Chatterbox #83 Round 9 — S3Gen UNet GPU drift on Metal
- L10802 — FA per-head additive mask CUDA kernel — what the upstream signature already gave us
- L11420 — funasr CUDA !-loop — all-NaN prefill logits (issue #125, §136)
- L12730 — Cohere flash-attn crossover
- L12790 — conv_transpose_1d GPU TDR — naive loop is O(IL), not O(K/s0)
- L12992 — flash_attn_ext vs scalar attention — numerical divergence
- L13103 — VibeVoice TTS garbles only on AMD RDNA4: coopmat2 flash-attention
- L14288 — Micro-optimizing an already-BLAS'd path with a GPU-ggml port is usually a dud — check for existing Accelerate/cblas and measure encode-vs-execute BEFORE porting
- L14611 — "Backend miscomputes my pipeline" ≠ "op X is broken": arbitrate with test-backend-ops, and beware aggregate precision (#304 native-Vulkan post-mortem)
- L16529 — Chatterbox GPU UNet performance fix
- L16768 — cosyvoice3 \"CUDA test FAIL\" was a registry-alias + flow-quant discovery bug, NOT a CUDA bug
- L18059 — "GPU picks a different token than CPU" is usually NOT a miscompute in an AR audio model — dump the LOGITS, and don't reach for the repetition detector

**Quantization** (10)

- L1885 — Auditing StelnetTTS against StelnetEmbed's bug classes: a weight reader with no quantized branch fails silently (2026-07)
- L1965 — Quantizing a forced-aligner: q8 everywhere (incl. lm_head) is bit-identical; q4 on the encoder is not (#192 TADA aligner)
- L3015 — Orpheus/SNAC TTS: `token_embd` must stay F16 for sub-Q8 quants
- L3617 — Quantisation and memory
- L3892 — Quantization
- L12721 — F16 precision loss in deep WaveNet stacks
- L12738 — MeloTTS + BERT quantization — what works and what doesn't
- L13832 — Re-quantized GGUFs on HF must be re-baked when the quantizer adds carve-outs
- L13920 — Sub-8-bit quantization of an audio tower fails as BEHAVIORAL collapse (loops, empty output) — and there is no per-block cliff to bisect
- L17556 — imatrix quant SHIPPED

**Perf measurement & A/B discipline** (8)

- L1559 — Per-step matvec dispatch overhead is LOAD-DEPENDENT — a "win" measured on a busy box is mostly a contention artifact
- L2029 — Best-of-N only helps if the scorer measures what you care about — TADA's reconstruction scorer is blind to duration outliers (#192)
- L3653 — CPU vs ONNX vs PyTorch baselines
- L4096 — Performance: what faster-whisper / insanely-fast-whisper do
- L5393 — 2026-04-23 - FireRed decoder optimization triage
- L11101 — Distinguishing "slow run" from "hung run" — CPU time ≪ wall time is the signal
- L11236 — Cross-backend bug-sweep methodology — pair the cap survey with an empirical A/B
- L13174 — §176 runtime optimization audit methodology

**Long-form audio, VAD & chunking** (8)

- L1790 — LLM-ASR (SALM) instruction-echo on short windows is genuine model behaviour, not a port bug — gate the input, don't chase the prompt
- L1843 — The Mimi codec transformer must be causal — non-causal silently truncates long audio; the >250-frame WER A/B settled it (2026-07)
- L3236 — VAD + chunking
- L4169 — VAD integration and long audio
- L10925 — TDT single-pass over a full long utterance is numerically fragile to codec-level audio noise
- L15033 — #227 "--vad-import wants ggml-tiny" = LID not VAD
- L15255 — #300 streaming diarization SHIPPED
- L15851 — #89 parakeet-ja long-form FIXED

**Tokenizers, prompts, language & text** (17)

- L702 — MOSS-TTS-Local 4B stop runaway was a PROMPT-TOKENIZATION bug — non-compositional BPE, not the forward (#249, 2026-07)
- L752 — Cross-lingual TTS needs the target language plumbed through /v1/audio/speech (#249/#304, 2026-07)
- L2041 — A converter that embeds a tokenizer MUST embed the merges — a silent `try/except: pass` byte-fallback breaks everything downstream (#192)
- L3739 — Language handling
- L5560 — FireRedPunc / fullstop-punc — BERT punctuation restoration
- L6565 — Chatterbox 24 kHz prompt mel — module 4 phase 3
- L7724 — Text LID via fastText — GlotLID-V3 + LID-176
- L7935 — Text LID via CLD3 — Google compact language detector
- L13025 — VibeVoice TTS: missing BPE merges still need BPE behavior
- L13555 — 2026-07 — SentencePiece tokenizer taxonomy: greedy longest-match is wrong for BOTH Unigram and BPE
- L13812 — SentencePiece `byte_fallback` is not optional decoration — OOV emoji/symbols must become `<0xHH>` byte tokens, and a `utf8_aligned` Viterbi dead-ends at a multi-byte lead without it
- L13961 — The prompt contract is part of the port: a specials-only tokenizer silently sent EVERY glm-asr prompt instruction-less
- L13996 — Unigram run-length loop metrics pass 2-gram cycles — and a "raw" baseline is only raw if the disable gate actually exists
- L15169 — #249 MOSS-Local 4B stop-runaway = PROMPT tokenization bug
- L15548 — #329 TTS target language
- L16622 — cohere language whitelist + probe LID
- L16740 — CV3 phase 6 speech_tokenizer_v3 ggml port

**Mel, codecs & the audio front-end** (15)

- L1779 — A one-frame error in a codec ENCODER shifts every RoPE position and shows up as an onset artifact, not a gradual drift — and a padding scheme faithful to one codec can be wrong for another
- L2643 — A glibc-only crash reproduces on macOS under AddressSanitizer — and the bug is often a non-power-of-two FFT (§205)
- L2934 — Streaming conv modules need cached left context, not zero-padding (#81)
- L3112 — FFT size must match upstream exactly (Mini-Omni2 / Whisper mel)
- L3421 — mel / preprocessor
- L3569 — Mel spectrograms
- L3707 — Audio format lessons
- L4678 — Kyutai STT: causal padding, interleaved RoPE, and codec-based ASR
- L4812 — FireRedVAD: FSMN Conv1d replication
- L6613 — Chatterbox atomic native voice clone — the resampler + 5-cond install
- L11590 — moshi / Mimi RVQ codebooks: decode uses embed_sum / cluster_usage
- L12613 — MeloTTS (VITS2) — from zero to BERT conditioning
- L15130 — #245/§232 cielvox2-tts CP_DIRECT + codec FASTCONV DONE
- L17455 — glint MP3+AAC TTS output §225 SHIPPED
- L17938 — voxcpm2 CausalTransposeConv1d kwargs gotcha

**Process, triage & documentation discipline** (19)

- L20 — One red job hides every job behind it — five days of a 33-job nightly that never ran
- L674 — When instrumenting the suspect file produces NO output, you are editing the wrong file — look for a second copy
- L767 — "The model emits it inline as text" is a claim to VERIFY, not to document — structured data you never parsed looks identical to a model limitation
- L1024 — A positional arg landing on the WRONG parameter is invisible in review and baked into the weights
- L1229 — PLAN "OPEN" items are frequently already shipped — audit against the CODE, never the prose
- L1467 — Audit a whole roadmap CLUSTER in one measure-first pass before implementing any of it
- L1773 — Handover docs can be dangerously stale — read the actual Python source, not the handover
- L2522 — DRY shared headers can exist for months before callers migrate (§175)
- L3815 — Regression testing discipline
- L3842 — Specific bugs that cost us a day each
- L4305 — CLI ↔ library DRY refactor
- L5869 — Audit script ≠ behavior test
- L11010 — Long-form ASR has three distinct failure classes, not one (2026-05-25, generalising issue #89)
- L12936 — beam_size default — greedy vs beam-5
- L13060 — VibeVoice TTS start clicks: distinguish decoder PCM from CLI post-processing
- L13608 — Issue #89 close-out: four transferable lessons
- L13903 — Issue triage discipline: check the codebase before leaving issues open
- L14530 — `git apply --3way` STAGES its result — a later `git add X && git commit` sweeps it up
- L16808 — StelnetEmbed #31 WASM OCR e2e

**Per-model port notes** (44)

- L522 — A model's capability list cannot be inferred from its vocabulary — and forcing a fake list does not simulate having the capability
- L3127 — Multi-stream token architecture (Mini-Omni2)
- L3777 — Model architecture comparisons
- L4721 — FireRedASR: Conformer encoder debugging
- L5674 — TTS / Vocoder (Chatterbox HiFTGenerator)
- L5936 — Chatterbox-Turbo conformer encoder — ggml layout traps
- L5987 — Chatterbox repaired GGUF split — stage-specific regen
- L6162 — Chatterbox voice cloning — bake to GGUF, load via `--voice`
- L6215 — Chatterbox VoiceEncoder native port — module 2 of voice cloning
- L6321 — Chatterbox S3Tokenizer V2 native port — module 3 of voice cloning
- L6490 — Chatterbox CAMPPlus phase 2 — TDNN forward
- L7386 — T5-family translation runtime traps (May 2026, MADLAD-400 debugging)
- L8092 — IndexTTS-1.5 TTS backend
- L8559 — Speaker verification — TitaNet
- L8607 — Parakeet-TDT greedy decode — blank + duration=0
- L8673 — VibeVoice 1.5B TTS voice cloning: acoustic + semantic dual encoder
- L9156 — SANM-encoder family (FunASR / SenseVoice / CosyVoice)
- L11549 — Round 10 — SpeechT5 + Dia TTS backend ports (2026-05-31/06-01)
- L11624 — Dia 1.6B TTS — what ACTUALLY fixed it
- L11678 — FastPitch TTS — non-autoregressive parallel TTS port (§133, 2026-06-02/03)
- L11844 — SpeechT5 TTS decoder — what ACTUALLY fixed it
- L11918 — Parler TTS — T5 + MusicGen decoder + DAC 44 kHz
- L12050 — MAES beam search for TDT transducers
- L12152 — Bark TTS — what ACTUALLY fixed it
- L12699 — OpenVoice2 voice cloning — ggml data layout
- L13437 — TADA encoder port: staged model loading on constrained RAM (§221)
- L14751 — #195 parakeet-ctc-1.1b-ja CTC routing
- L14794 — #205 --max-len text-split + granite-plus
- L14911 — #221 irodori voice cloning SHIPPED
- L15074 — #231 cohere-arabic loop = corrupt GGUF
- L15187 — #249 MOSS voice cloning SHIPPED
- L15235 — vibevoice #299 runtime-reproduced
- L15300 — #308 whisper subtitles double-caps
- L15781 — #334 cosyvoice3 clone bugs
- L15919 — #93 voxtral-tts SHIPPED
- L16399 — ARK-ASR-3B port SHIPPED
- L16903 — CSM-1B TTS §135 FIXED
- L16947 — Dia 1.6B TTS port
- L17123 — dots.tts #200 SHIPPED
- L17496 — higgs-audio-v3-stt SHIPPED
- L17627 — indextts long-ref crash FIXED
- L17866 — TADA TTS time embedding bugs
- L17914 — Four bugs that together caused empty/garbage transcripts; all patched in src/vibevoice.cpp +…
- L18139 — "Verified byte-identical at 225 s" verified a clip, not a length — and a decoder that drops spans needs a repair pass, not a better cap

## Cross-reference by model / family

A section is listed when its heading names the family, or its body mentions it
at least 3 times. Sections appear under several families; this is a
lookup, not a partition.

- **chatterbox** (26) — L2458, L2643, L5674, L5936, L5987, L6019, L6058, L6162, L6215, L6321, L6424, L6490, L6565, L6613, L8762, L9549, L13174, L14222, L14611, L14671, L15130, L15919, L16529, L16567, L17123, L18197
- **cosyvoice3** (9) — L842, L9156, L13174, L14222, L14671, L15548, L15781, L16740, L16768
- **vibevoice** (14) — L767, L1918, L2825, L5393, L6162, L8673, L13025, L13060, L13103, L13779, L15235, L15255, L15486, L17914
- **tada** (9) — L1248, L1621, L1965, L2015, L2029, L13174, L13437, L16163, L17866
- **whisper** (22) — L442, L2134, L3112, L3912, L4096, L4169, L4305, L4812, L5753, L7724, L8092, L12936, L14794, L15300, L15343, L15409, L15851, L16399, L16740, L17496, L17556, L17672
- **parakeet / TDT** (18) — L213, L975, L2134, L2167, L3236, L4169, L4515, L6613, L8607, L10925, L11010, L12050, L13608, L14751, L14794, L15851, L17672, L18139
- **moss** (13) — L702, L898, L1390, L1592, L1767, L1773, L2064, L2089, L2114, L3236, L14414, L15169, L15187
- **firered** (9) — L1559, L4721, L4812, L5393, L5560, L6424, L13555, L15300, L15343
- **funasr / sensevoice / SANM** (2) — L9156, L11420
- **kyutai / moshi / mimi** (7) — L1779, L1843, L4678, L11590, L12364, L12520, L16903
- **dia** (5) — L1521, L11549, L11624, L11844, L16947
- **parler** (1) — L11918
- **speecht5** (2) — L11549, L11844
- **bark** (1) — L12152
- **pocket-tts** (3) — L12364, L12520, L17741
- **melotts / VITS** (5) — L1685, L12613, L12738, L13153, L16221
- **fastpitch** (1) — L11678
- **indextts** (1) — L8092
- **orpheus / SNAC** (1) — L3015
- **voxcpm2** (4) — L8762, L8966, L9089, L17938
- **openvoice2** (1) — L12699
- **glm-asr** (1) — L13961
- **qwen3** (17) — L1592, L3466, L4169, L4812, L7491, L8092, L8762, L9156, L11138, L13920, L14018, L14111, L14794, L15130, L15548, L17556, L18059
- **madlad / T5** (1) — L7386
- **mini-omni2** (2) — L3112, L3127
- **titanet / speaker-id** (1) — L8559
- **audioseal** (1) — L12759
- **cohere** (7) — L522, L3569, L11074, L12730, L14794, L15074, L16622
- **voxtral** (11) — L3466, L3569, L3617, L3777, L4169, L11010, L11074, L11101, L11236, L15919, L17123
- **granite** (3) — L2209, L3466, L14794
- **ark-asr** (3) — L61, L16399, L17123
- **higgs-stt** (2) — L898, L17496
- **dots-tts** (2) — L1773, L17123
- **irodori** (2) — L1650, L14911
- **f5-tts** (1) — L14253
- **kokoro** (4) — L615, L7491, L15548, L16221
- **zonos** (1) — L11918
- **omniasr / omnivoice** (6) — L442, L1345, L4812, L14050, L14078, L14464
- **glm / mimo / mega** (3) — L11266, L13961, L14018
